import { Component } from 'react';
import dashboardTemplate from '../views/dashboard';

class Dashboard extends Component {
  render() {
    return dashboardTemplate(this);
  }
}

export default Dashboard;
